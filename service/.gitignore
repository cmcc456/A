# 日志文件  
logs/  
*.log  
npm-debug.log*  
yarn-debug.log*  
yarn-error.log*  
pnpm-debug.log*  
lerna-debug.log*  
  
# 依赖和构建输出目录  
node_modules/  
dist/  
dist-ssr/  
coverage/  
*.local  
  
# Cypress 测试相关的视频和截图  
/cypress/videos/  
/cypress/screenshots/  
  
# 编辑器相关目录和文件  
.vscode/  
!.vscode/settings.json  
!.vscode/extensions.json  
.idea/  
*.suo  
*.ntvs*  
*.njsproj  
*.sln  
*.sw?  
  
# 构建输出目录  
build/  
  
# macOS 隐藏文件  
.DS_Store  
  
# 注释说明  
# 日志文件通常包含敏感信息或大量运行时的数据，不需要版本控制。  
# 依赖和构建输出目录是自动生成的，不需要版本控制。  
# Cypress 测试相关的视频和截图通常很大，且包含测试运行时的具体状态，不需要版本控制。  
# 编辑器相关目录和文件包含个人配置和临时文件，不应该被版本控制。  
# 构建输出目录是构建过程的产物，不需要版本控制。  
# macOS 隐藏文件是系统生成的，不应该被版本控制。